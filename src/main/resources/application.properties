spring.application.name=dbschema

# Database configuration
spring.datasource.jdbc-url=jdbc:postgresql://localhost:5433/dbschema
spring.datasource.username=postgres
spring.datasource.password=551551551
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect (already commented out, which is fine)
spring.jpa.properties.hibernate.format_sql=true

# Server configuration
server.port=8080

# Logging configuration
logging.level.com.dbschema.dbschema=DEBUG